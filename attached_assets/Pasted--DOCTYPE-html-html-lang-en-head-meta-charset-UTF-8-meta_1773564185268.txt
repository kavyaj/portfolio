<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>What I'm Building — Kavya</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">

<style>
  :root {
    --primary-pink: #fd5183;
    --black: #121212;
    --white: #fff;
    --gray-light: #f5f5f5;
    --gray-medium: #666;
    --gray-dark: #333;
    --gray-divider: #e0e0e0;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--white);
    color: var(--black);
    padding: 5rem 0;
  }

  /* ── SECTION WRAPPER ── */
  .section {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 3rem;
  }

  /* ── SECTION HEADER ── */
  .section-header {
    display: flex;
    align-items: baseline;
    gap: 1.5rem;
    margin-bottom: 3.5rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--gray-divider);
  }

  .section-title {
    font-family: 'DM Serif Display', serif;
    font-size: 2.4rem;
    letter-spacing: -0.02em;
    color: var(--black);
    line-height: 1;
  }

  .section-title em {
    font-style: italic;
    color: var(--primary-pink);
  }

  .section-count {
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gray-medium);
  }

  /* ── ITEMS ── */
  .items {
    display: flex;
    flex-direction: column;
  }

  .item {
    display: grid;
    grid-template-columns: 200px 1fr auto;
    gap: 2rem;
    align-items: start;
    padding: 2.2rem 0;
    border-bottom: 1px solid var(--gray-divider);
    text-decoration: none;
    color: inherit;
    transition: background 0.2s;
    position: relative;
  }

  .item:first-child {
    border-top: none;
  }

  .item::before {
    content: '';
    position: absolute;
    left: -3rem;
    right: -3rem;
    top: 0;
    bottom: 0;
    background: var(--gray-light);
    opacity: 0;
    transition: opacity 0.2s;
    z-index: 0;
  }

  .item:hover::before { opacity: 1; }

  .item > * { position: relative; z-index: 1; }

  /* ── ITEM LEFT ── */
  .item-left {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-top: 0.2rem;
  }

  .item-type {
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gray-medium);
  }

  .item-name {
    font-family: 'DM Serif Display', serif;
    font-size: 1.5rem;
    letter-spacing: -0.02em;
    color: var(--black);
    line-height: 1.1;
  }

  .item-name.pink { color: var(--primary-pink); }

  .item-tag {
    display: inline-block;
    font-size: 0.68rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.25rem 0.65rem;
    border-radius: 100px;
    border: 1px solid var(--gray-divider);
    color: var(--gray-medium);
    width: fit-content;
    margin-top: 0.25rem;
  }

  .item-tag.book {
    border-color: rgba(253, 81, 131, 0.3);
    color: var(--primary-pink);
    background: rgba(253, 81, 131, 0.05);
  }

  /* ── ITEM MIDDLE ── */
  .item-body {
    font-size: 0.95rem;
    line-height: 1.8;
    color: var(--gray-medium);
    font-weight: 300;
    max-width: 55ch;
    padding-top: 0.2rem;
  }

  .item-body strong {
    color: var(--gray-dark);
    font-weight: 500;
  }

  /* ── ITEM RIGHT ── */
  .item-right {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    justify-content: space-between;
    height: 100%;
    gap: 1rem;
    padding-top: 0.2rem;
  }

  .item-arrow {
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    border: 1.5px solid var(--gray-divider);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.85rem;
    color: var(--gray-medium);
    transition: border-color 0.2s, color 0.2s, transform 0.2s;
    flex-shrink: 0;
  }

  .item:hover .item-arrow {
    border-color: var(--primary-pink);
    color: var(--primary-pink);
    transform: translate(2px, -2px);
  }

  .item-year {
    font-size: 0.72rem;
    color: var(--gray-medium);
    letter-spacing: 0.06em;
    font-weight: 400;
  }

  /* ── FOOTER NOTE ── */
  .section-footer {
    margin-top: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .section-note {
    font-size: 0.82rem;
    color: var(--gray-medium);
    font-style: italic;
    font-weight: 300;
  }

  /* ── RESPONSIVE ── */
  @media (max-width: 768px) {
    .section { padding: 0 1.5rem; }
    .item { grid-template-columns: 1fr; gap: 1rem; }
    .item-right { flex-direction: row; align-items: center; }
    .section-header { flex-direction: column; gap: 0.5rem; }
  }
</style>
</head>
<body>

<div class="section">

  <!-- Header -->
  <div class="section-header">
    <h2 class="section-title">What I'm <em>building</em></h2>
    <span class="section-count">3 things</span>
  </div>

  <!-- Items -->
  <div class="items">

    <!-- Resumey.Pro -->
    <a href="https://resumey.pro" target="_blank" class="item">
      <div class="item-left">
        <span class="item-type">SaaS · Resume Builder</span>
        <span class="item-name pink">Resumey.Pro</span>
        <span class="item-tag">Live</span>
      </div>
      <p class="item-body">
        A Markdown resume builder for people who want greater control for resume-building. Built in 2019 out of genuine frustration — every other tool was either too rigid or too complicated. <strong>Turns out we weren't the only ones.</strong>
      </p>
      <div class="item-right">
        <span class="item-arrow">↗</span>
        <span class="item-year">Since 2019</span>
      </div>
    </a>

    <!-- StoryCV -->
    <a href="https://storycv.com" target="_blank" class="item">
      <div class="item-left">
        <span class="item-type">SaaS · Resume Writer</span>
        <span class="item-name pink">StoryCV</span>
        <span class="item-tag">Live</span>
      </div>
      <p class="item-body">
        Most people know what work they've done. They just go blank when asked to write it down. StoryCV is the digital equivalent of a human resume writer — it takes your real experience and turns it into <strong>concise, effective bullet points.</strong>
      </p>
      <div class="item-right">
        <span class="item-arrow">↗</span>
        <span class="item-year">Since 2023</span>
      </div>
    </a>

    <!-- The Brave Rocketeers -->
    <a href="https://www.amazon.sg/Brave-Rocketeers-conquer-their-fears/dp/B0DDHC7RVZ" target="_blank" class="item">
      <div class="item-left">
        <span class="item-type">Children's Book</span>
        <span class="item-name">The Brave<br>Rocketeers</span>
        <span class="item-tag book">On Amazon</span>
      </div>
      <p class="item-body">
        A children's book about courage and curiosity, written by a sleep-deprived new mother who apparently needed more challenges in her life. Joey and Avi go to space, face their fears, and come back braver — <strong>dedicated to my son.</strong>
      </p>
      <div class="item-right">
        <span class="item-arrow">↗</span>
        <span class="item-year">2024</span>
      </div>
    </a>

  </div>

  <!-- Footer note -->
  <div class="section-footer">
    <p class="section-note">I couldn't find the things I needed. So I made them.</p>
  </div>

</div>

</body>
</html>